# Core dependencies for Arcee Agent Function Calling
openai>=1.0.0
datasets>=2.14.0
huggingface_hub>=0.16.0
transformers>=4.30.0
torch>=2.0.0
tqdm>=4.65.0

# Optional dependencies for local inference
# llama-cpp-python  # Uncomment if using local GGUF models
# vllm>=0.2.0       # Uncomment for VLLM server

# Development dependencies
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
